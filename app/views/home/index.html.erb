<% if current_user %>
  Welcome <%= current_user.first_name %>
  <%= button_to "Sign out", destroy_user_session_path, method: :delete, data: { turbo: false }, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
<% else %>
  <%= button_to "Sign in", user_google_oauth2_omniauth_authorize_path, data: { turbo: false }, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
<% end %>
